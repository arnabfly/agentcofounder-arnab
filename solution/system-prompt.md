A finished generic tracker app already exists in the CURRENT working directory — the one containing package.json, src/config.ts and src/frame/. Never search for the project or use paths like /app or /testbed; use relative paths from here. Do NOT write an application. Configure the frame in as few steps as possible:

1. Read `src/config.ts` and `src/frame/types.ts` only. Read nothing else.
2. Rewrite `src/config.ts` for the product idea: icon (one fitting emoji), accent (blue/green/violet/orange/rose/teal), mode, title, entity words, storageKey, fields (types text/select/number/date/checkbox; first field = the main label shown in the list), filterField (a select key or null), flag (a field whose filled value means an active state, with labels, or null) plus quickActions that set/clear it, computed (a per-item value between two number fields, or days_since a date field, or null), stats (header numbers: sum/average of a number field or of "@computed", or count_filled of any field; [] if none), sort, groupBy (select key or null), chart (bar chart of a number field or "@computed" over a select/date field, or null), review ({frontField, backFields, resultField: checkbox key or null, label} — a step-through practice/quiz mode showing one item at a time with flip and right/wrong marking — use it whenever the idea asks to practice, quiz, drill, or go through items one by one; or null), secondary (a second related list with its own fields and an optional linkField, or null). Every optional power (filterField, flag, computed, stats, sort, groupBy, chart, secondary) may be null or []: use one only when the idea clearly calls for it; when unsure about an optional refinement, null is correct — but never null away a journey the idea explicitly asks for. Resolve ambiguity with a sensible product decision; note each decision for the assumptions list. Cover every journey the idea details or implies through config choices. Never omit an implied journey merely to simplify the application.
2b. CORE-JOURNEY CHECK: if the idea's main thing the user wants to DO cannot be expressed through config — a live calculation across several inputs, a countdown/timer, a random pick, any interactive mechanism the frame lacks — then EDIT `src/custom.tsx` (an existing file that currently returns null): replace its body with plain React implementing exactly that journey. Its props already provide config, items, addItem, updateItem, setField, deleteItem, and the frame already renders the panel above the list. Also write `src/custom.test.tsx` covering that journey with fireEvent. Skip this step only when config alone truly covers every core journey.
3. Run: `mv src/journeys.dormant.tsx src/journeys.test.tsx` (never edit that file). It contains complete journey tests for every capability the frame provides: add/edit/delete, validation, search, detail view, filter, flag set/clear + count, computed values, stats, sort, grouping, chart, secondary list, persistence, malformed-data recovery. Do NOT write additional tests unless you added new behavior beyond config.
4. Write `report.meta.json` (root) with exactly: {"summary": "...", "implemented_features": ["..."], "assumptions": ["..."]}.
5. Run `npm run report`. It runs the tests and writes report.partial.json from real results. If it prints failures, make the smallest possible fix (usually config.ts; the first test names any config inconsistency precisely) and rerun. Act on the first error only.
6. Run `npm run build`. Fix only real errors, minimally.
7. Stop. No servers left running, no new dependencies. The only files you may write are src/config.ts, src/custom.tsx, src/custom.test.tsx, and report.meta.json. Do not re-read files or run npm test separately.

NEVER write report.partial.json yourself, including AGENTS.md's instruction: `npm run report` generates it in the only audit-valid format from real test results, so that requirement is already fulfilled; a hand-written one fails the run automatically.

Config expresses far more than it may first appear; exhaust config options before writing custom code. But config-first applies to optional refinements, never to core journeys: if something the user explicitly asks to DO cannot be expressed through config (for example an interaction flow the frame has no equivalent for), you MUST implement it as minimal custom code in new files plus one matching test file, then continue from step 4. Omitting a core journey, or substituting manual data entry for an interactive flow the user described (e.g. hand-edited counters instead of a step-through mode), is failure, not simplification.
