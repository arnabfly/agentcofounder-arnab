A finished generic tracker app already exists here. Do NOT write an application. Configure the frame in as few steps as possible:

1. Read `src/config.ts` and `src/frame/types.ts` only. Read nothing else.
2. Rewrite `src/config.ts` for the product idea: icon (one fitting emoji), accent (blue/green/violet/orange/rose/teal), mode, title, entity words, storageKey, fields (types text/select/number/date/checkbox; first field = the main label shown in the list), filterField (a select key or null), flag (a field whose filled value means an active state, with labels, or null) plus quickActions that set/clear it, computed (a per-item value between two number fields, or days_since a date field, or null), stats (header numbers: sum/average of a number field or of "@computed", or count_filled of any field; [] if none), sort, groupBy (select key or null), chart (bar chart of a number field or "@computed" over a select/date field, or null), secondary (a second related list with its own fields and an optional linkField, or null). Resolve ambiguity with a sensible product decision; note each decision for the assumptions list. Cover every journey the idea details or implies through config choices. Never omit an implied journey merely to simplify the application.
3. Run: `mv src/journeys.dormant.tsx src/journeys.test.tsx` (never edit that file). It contains complete journey tests for every capability the frame provides: add/edit/delete, validation, search, detail view, filter, flag set/clear + count, computed values, stats, sort, grouping, chart, secondary list, persistence, malformed-data recovery. Do NOT write additional tests unless you added new behavior beyond config.
4. Write `report.meta.json` (root) with exactly: {"summary": "...", "implemented_features": ["..."], "assumptions": ["..."]}.
5. Run `npm run report`. It runs the tests and writes report.partial.json from real results. If it prints failures, make the smallest possible fix (usually config.ts; the first test names any config inconsistency precisely) and rerun. Act on the first error only.
6. Run `npm run build`. Fix only real errors, minimally.
7. Stop. No servers left running, no new dependencies, no other files changed. Do not re-read files or run npm test separately.

NEVER write report.partial.json yourself, including AGENTS.md's instruction: `npm run report` generates it in the only audit-valid format from real test results, so that requirement is already fulfilled; a hand-written one fails the run automatically.

Config expresses far more than it may first appear; exhaust config options before considering any custom code. Only if the idea truly requires behavior the frame cannot express through config: add minimal new code in new files plus one matching test file, then continue from step 4.
